{
  "name": "fus",
  "version": "1.1.0",
  "private": true,
  "description": "Claude Code 通用开发工具集插件",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.9",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
