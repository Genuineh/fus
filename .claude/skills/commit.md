# commit

智能提交代码，生成符合 Conventional Commits 规范的 commit message。

## 使用方法

直接输入 `/commit` 或带参数 `/commit <message>`

## 执行

```bash
{{root}} && node /home/jerryg/github/fus/packages/fus-skills/dist/skills/commit/index.js
```
